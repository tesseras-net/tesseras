#!/bin/ksh

daemon="/usr/local/bin/tes"
daemon_flags="--identity=/var/lib/tesseras admin daemon start --foreground"
daemon_logger="daemon.info"
daemon_user="_tesseras"

. /etc/rc.d/rc.subr

pexp="/usr/local/bin/tes.*admin daemon.*"
rc_bg=YES
rc_reload=NO

rc_cmd $1
