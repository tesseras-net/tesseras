[private]
default:
    @just --list --unsorted

# serve the site
serve:
    zola serve

# build the site
build:
    zola build

# deploy the site
deploy server="tesseras-website":
    ./deploy.sh {{ server }}
