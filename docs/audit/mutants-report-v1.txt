# Mutation Testing Report v1 (updated after gap-filling)
# Date: 2026-02-15
# cargo-mutants version: 26.2.0
# Files: dual.rs, sealed.rs

## dual.rs — PASS

Found 3 mutants to test
3 mutants tested: 2 caught, 1 unviable
Result: ALL CAUGHT (1 unviable = compile error, not a test gap)

## sealed.rs — PASS (1 benign equivalent)

Found 16 mutants to test

### After gap-filling:
16 mutants tested: 1 missed, 13 caught, 2 unviable

### Remaining missed mutation (benign equivalent):
MISSED: sealed.rs:54:27: replace < with <= in SealedKeyEnvelope::unseal

This is a benign equivalent mutation: changing `ct_bytes.len() < 32` to `<= 32`
makes the guard catch the `== 32` case instead of letting it through to
`HybridKem::decapsulate` which would also fail. Both paths produce the same
`Err(CryptoError::DecryptFailed)` — the behavior is identical. Any valid sealed
ciphertext is >> 32 bytes (ML-KEM ciphertext alone is ~1088 bytes).

### Gaps filled:
- Added: unseal_rejects_short_ciphertext (kills `< → ==`)
- Added: unseal_rejects_exactly_31_bytes_ciphertext
- Added: key_publication_tampered_tessera_hash_fails (kills signing_message → vec![])
- Added: key_publication_tampered_content_key_fails (kills signing_message → vec![0])
- Added: key_publication_tampered_timestamp_fails (kills signing_message → vec![1])
